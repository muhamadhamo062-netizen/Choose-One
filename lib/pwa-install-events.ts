/**
 * Window events for PWA install gating (no business logic; analytics + UI only).
 */
export const PWA_EVENT_SCAN_COMPLETE = "privacyeraser:scan-complete" as const;
export const PWA_EVENT_PAYWALL_VIEWED = "privacyeraser:paywall-viewed" as const;
export const PWA_EVENT_DASHBOARD_VIEWED = "privacyeraser:dashboard-viewed" as const;
